# Zaga Code

## TODO

- [ ] Create node for project indexing caching somewhere faster to access instead of using the database for every tool call.
- [ ] Project inferrence (language, framework, etc.)
