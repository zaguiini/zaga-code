# Zaga Code

## TODO

- [ ] Implement RAG indexing in the background
- [ ] Allow customizing the projectPath through context and refactor tools to use the context
- [ ] Extract models and runtime configuration to an env variable, and verify everything's there before running the agent
- [ ] Project inferrence (language, framework, etc.)
