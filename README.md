# Zaga Code

## TODO

- [ ] Move away from ReAct abstraction. Once that's done, create nodes for thread naming and project indexing caching in memory instead of using the database for every tool call.
- [ ] Project inferrence (language, framework, etc.)
